import { FileJson, FileJson2 } from "lucide-react";

const Header = () => (
  <h1 className="text-4xl font-bold flex items-center gap-2">
    <span>JSON Tamer</span> <FileJson2 className="size-10" />
  </h1>
);

export default Header;
